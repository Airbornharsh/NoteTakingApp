export function classDepth(obj) {
    const proto = Object.getPrototypeOf(obj);
    if (!proto) {
        return 0;
    }
    return 1 + classDepth(proto);
}
export function sortClasses(classes) {
    return [
        ...classes
    ].sort((a, b) => {
        const depthA = classDepth(a);
        const depthB = classDepth(b);
        if (depthA > depthB) {
            return -1;
        }
        if (depthB > depthA) {
            return 1;
        }
        return 0;
    });
}
//# sourceMappingURL=sort-classes.js.map
