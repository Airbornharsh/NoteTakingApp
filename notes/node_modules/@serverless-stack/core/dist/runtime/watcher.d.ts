import { EventDelegate } from "../events.js";
declare type Event = {
    files: string[];
};
export declare class Watcher {
    readonly onChange: EventDelegate<Event>;
    private chokidar?;
    reload(root: string): void;
}
export {};
