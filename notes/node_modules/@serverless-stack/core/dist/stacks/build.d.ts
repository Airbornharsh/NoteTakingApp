import { Config } from "../config";
import ts from "typescript";
import type { Diagnostic } from "typescript";
export declare function build(root: string, config: Config): Promise<void>;
export declare function check(root: string, config: Config): ts.Diagnostic[];
export declare function formatDiagnostics(list: Diagnostic[]): string[];
