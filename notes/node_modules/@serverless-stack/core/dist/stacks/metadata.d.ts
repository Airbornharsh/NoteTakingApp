import { CloudFormationStackArtifact } from "aws-cdk-lib/cx-api";
import { Config } from "../config/index.js";
interface Manifest {
    version: string;
    artifacts: Record<string, Artifact>;
}
declare type Artifact = StackArtifact;
interface StackArtifact {
    type: "aws:cloudformation:stack";
    properties: CloudFormationStackArtifact;
    displayName: string;
}
export declare function manifest(root: string): Promise<Manifest>;
export declare function metadata(root: string, config: Config): Promise<any[]>;
export {};
