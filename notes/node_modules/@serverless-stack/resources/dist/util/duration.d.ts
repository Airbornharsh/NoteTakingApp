import * as cdk from "aws-cdk-lib";
export declare type Duration = `${number} ${"second" | "seconds" | "minute" | "minutes" | "hour" | "hours" | "day" | "days"}`;
export declare function toCdkDuration(duration: Duration): cdk.Duration;
